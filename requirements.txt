azure-functions>=1.21.0
azure-functions-durable>=1.2.9
azure-search-documents>=11.5.1
# Managed Identity auth for SharePoint/Graph — see sharepoint_client.py's
# module docstring. Replaced a stored client secret entirely.
azure-identity>=1.17.0
requests>=2.32.0
pdfplumber>=0.11.0
python-docx>=1.1.0
trafilatura>=1.12.0
# trafilatura's dependency chain (via courlan) imports lxml.html.clean,
# which lxml itself split out into this separate package as of lxml 5.2+.
# Pin it explicitly — Oryx's remote build resolves the latest lxml, which
# no longer bundles it, and the import crashes the whole worker at startup
# (not just this feature) if it's missing.
lxml_html_clean>=0.3.0
markdown>=3.7
python-pptx>=1.0.2
beautifulsoup4>=4.12.0
rapidfuzz>=3.0.0
# Async client for the self-hosted SearXNG search instance (searxng-app/)
# used by live_url_search.py — replaced ddgs (DuckDuckGo scraper), which
# proved unreliable under sustained real-world load.
httpx>=0.27.0
